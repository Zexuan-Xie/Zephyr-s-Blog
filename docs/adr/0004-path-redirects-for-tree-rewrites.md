# Preserve links with path redirects

The content tree allows directories and files to be renamed or moved, so public paths can change over time. The first release will record `path_redirects` for published files affected by slug or location changes, trading a moderate amount of redirect bookkeeping for stable external links and the ability to reorganize the Unix-like content tree.
