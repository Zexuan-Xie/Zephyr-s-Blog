# Scope uploaded assets to files

The first release will support per-file assets so Markdown files and full HTML documents can reference images, scripts, styles, and data files without relying on external URLs. Assets are scoped to a single file rather than managed as a global media library, trading some upload and MIME-safety complexity for self-contained content and cleaner long-term ownership.
