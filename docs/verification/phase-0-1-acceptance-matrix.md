# Phase 0/1 Verification Acceptance Matrix

Generated by worker-4 on 2026-06-03 for the OMX task: verification/contract lane.

## Scope and Sources

- Implementation plan: `IMPLEMENTATION_PLAN.md`
- Product acceptance: `docs/specs/PRD.md`
- Stack/version contract: `docs/specs/TECH_STACK.md`
- Backend/API contract: `docs/specs/BACKEND_STRUCTURE.md`, `docs/api/openapi.yaml`
- Active context snapshot: `.omx/context/xlab-blog-implementation-start-20260603T043946Z.md`
- Integrated leader tree inspected read-only at `/home/zephry_xzx/xlab/blog` after team auto-checkpoints through `7c2a920`.

## Summary

The OpenAPI document is syntactically parseable and all local `$ref` targets resolve. Phase 0/1 implementation artifacts are not present in the integrated tree yet, so backend, frontend, Docker, and end-to-end acceptance checks are currently blocked by missing files rather than failing implementation behavior.

## Contract Checks

| Check | Result | Evidence | Gap / Next Action |
|---|---:|---|---|
| OpenAPI YAML parses | PASS | Python `yaml.safe_load('docs/api/openapi.yaml')` succeeded | Keep `docs/api/openapi.yaml` authoritative before any API shape changes. |
| OpenAPI local `$ref` targets resolve | PASS | Custom Python ref walk reported `PASS all local $refs resolve` | Run a formal OpenAPI validator when available. |
| OpenAPI surface exists for required domains | PASS | 22 paths present, including auth, public tree, search, comments, likes, assets, admin tree/search endpoints | Backend handlers are not implemented yet. |
| Exact stack contract can be verified from files | FAIL | `api/go.mod` and `web/package.json` missing | Add backend/frontend manifests with exact versions from `TECH_STACK.md`. |
| Root environment contract | FAIL | `environment.yml` and `.env.example` missing | Add `environment.yml` with `name: blogenv` and safe env template. |
| Deployment skeleton | FAIL | `docker-compose.yml` and `Caddyfile` missing | Add Compose v2 config and Caddy reverse proxy/static serving skeleton. |

## Artifact Presence Matrix

| Required artifact | Required by | Current state | Status |
|---|---|---|---:|
| `environment.yml` with `name: blogenv` | `IMPLEMENTATION_PLAN.md`, `TECH_STACK.md` | Missing | FAIL |
| `.env.example` | Backend/env bootstrap | Missing | FAIL |
| `api/go.mod` | Go/chi backend foundation | Missing | FAIL |
| `api/internal/**` | Backend layering and services | Missing | FAIL |
| `api/migrations/**` | DB schema and pgvector/extensions | Missing | FAIL |
| `web/package.json` | Vite React SPA foundation | Missing | FAIL |
| `web/src/**` | SPA routes and UI shell | Missing | FAIL |
| `docker-compose.yml` | Deployability acceptance | Missing | FAIL |
| `Caddyfile` | SPA/API edge acceptance | Missing | FAIL |
| `docs/api/openapi.yaml` | API contract source of truth | Present; parse/ref check passes | PASS |

## Local Toolchain Matrix

| Tool | Required | Observed | Status |
|---|---:|---:|---:|
| Node.js | `22.22.3` | `v22.22.2` | FAIL |
| npm | `10.9.8` | `10.9.7` | FAIL |
| Go | `1.26.4` | command not found | FAIL |
| Docker Compose | v2 via `docker compose` | Docker command unavailable in WSL distro | FAIL |
| Conda | local env manager | `conda 26.1.1` | PASS |

These toolchain mismatches should be treated as environment blockers, not grounds for substituting package or language versions.

## PRD Acceptance Matrix

| PRD acceptance signal | Current result | Evidence / blocker |
|---|---:|---|
| Docker Compose starts web/api/db; Caddy serves SPA and proxies `/api` | BLOCKED | No `docker-compose.yml`; Docker command unavailable. |
| Admin seed login and `/admin` content creation | BLOCKED | No backend, DB migrations, frontend, or admin UI artifacts. |
| Root/nested/File SPA path resolution | BLOCKED | No `web/` SPA or `/api/tree/resolve` implementation. |
| Published path changes create `path_redirects` and resolve redirects | BLOCKED | No migrations/repositories/services. |
| Markdown File renders body, keyword chips, likes, comments | BLOCKED | No renderer/UI/API implementation. |
| HTML Document uses iframe sandbox `allow-scripts` without `allow-same-origin` | BLOCKED | No HTML renderer/UI implementation. |
| Assets upload/public access/cache/MIME/SVG/PDF rules | BLOCKED | No asset storage, API, or validation implementation. |
| `/search` returns path/snippet/source badge; embedding failure falls back to full-text | BLOCKED | No backend search implementation or frontend search page. |
| Anonymous read; interaction redirects to login and returns to target or `/recent` | BLOCKED | No auth/frontend route implementation. |
| Five high-risk tests: JWT, like idempotency, Markdown XSS, iframe sandbox, SVG safety | BLOCKED | No test suites or implementation artifacts yet. |

## Verification Commands Run

Commands were run against the integrated leader tree at `/home/zephry_xzx/xlab/blog`.

```text
Artifact presence:
FAIL missing environment.yml
FAIL missing .env.example
FAIL missing docker-compose.yml
FAIL missing Caddyfile
FAIL missing api/go.mod
FAIL missing web/package.json
PASS exists docs/api/openapi.yaml

Tool versions:
node --version -> v22.22.2
npm --version -> 10.9.7
go version -> command not found
docker compose version -> docker command unavailable
conda --version -> conda 26.1.1

Checks:
cd api && go test ./... -> FAIL (api directory missing)
cd web && npm run lint && npm run build -> FAIL (web directory missing)
docker compose config -> FAIL (docker unavailable / compose file missing)
OpenAPI Python YAML/ref check -> PASS (22 paths, 33 schemas, all local refs resolve)
```

## Recommended Next Feasible Work

1. Complete Packet A root foundation first: `environment.yml`, `.env.example`, `docker-compose.yml`, `Caddyfile`.
2. Complete Packet B/C backend foundation: `api/go.mod`, config, DB pool, migrations, health endpoint, and tests with exact versions from `TECH_STACK.md`.
3. Complete Packet D/E frontend foundation: `web/package.json`, Vite React routes, design tokens, iframe/Markdown skeleton, and exact package versions.
4. Re-run this matrix after the implementation artifacts exist; expected minimum commands are `cd api && go test ./...`, `cd web && npm run lint && npm run build`, `docker compose config`, and health smoke where Docker is available.
