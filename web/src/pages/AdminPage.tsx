export function AdminPage() {
  return (
    <section className="glass status-panel">
      <p className="eyebrow">ADMIN</p>
      <h1>Tree Manager foundation</h1>
      <p>
        Admin CRUD, file editor, assets, publish controls, and embedding actions belong to the Packet I vertical slice.
        This route is reserved now so auth redirects and navigation are stable.
      </p>
    </section>
  );
}
